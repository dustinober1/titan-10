# Architecture

> Auto-generated by /map on 2026-01-18

## Overview

Titan-10 is an autonomous cryptocurrency market data ingestion and analysis system. It is designed to fetch market data from various exchanges, normalize it, and store it for analysis.

```
┌─────────────────────────────────────────┐
│               [Exchanges]               │
│             (CCXT / APIs)               │
├─────────────────────────────────────────┤
│              [Ingestor]                 │
│      (Fetcher -> Normalizer)            │
├─────────────────────────────────────────┤
│               [Storage]                 │
│              (Postgres)                 │
└─────────────────────────────────────────┘
```

## Components

### Ingestor
- **Purpose**: Core logic for fetching and processing market data.
- **Location**: `src/ingestor`
- **Sub-components**:
  - `exchange.py`: Interface with cryptocurrency exchanges (likely wrapping CCXT).
  - `fetcher.py`: retrieval logic.
  - `normalizer.py`: Standardizing data formats.
  - `scheduler.py`: Orchestrating ingestion jobs (APScheduler).
  - `backfill.py`: Handling historical data retrieval.

### Storage
- **Purpose**: Data persistence and schema management.
- **Location**: `src/storage`
- **Dependencies**: `asyncpg`, PostgreSQL.

### Shared
- **Purpose**: Cross-cutting concerns.
- **Location**: `src/shared`
- **Components**: `config.py` (Pydantic settings), `logging.py`.

## Data Flow

1. **Scheduler** triggers **Fetcher** jobs.
2. **Fetcher** uses **Exchange** adapter to request data from external APIs (via CCXT).
3. Raw data is passed to **Normalizer**.
4. Normalized data is persisted via **Storage** layer to PostgreSQL.

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| Crypto Exchanges | API | Source of market data (via CCXT) |
| PostgreSQL | Database | Persistent storage of time-series data |

## Technical Debt

- [ ] **Tests**: explicit `tests/` directory was not immediately verified in top-level listing (needs confirmation).
- [ ] **Documentation**: Limited to README.md and auto-generated docs.

## Conventions

**Naming:** Python snake_case.
**Structure:** Modular `src/` layout with domain-based packages.
**Configuration:** Environment variables via `pydantic-settings` and `.env`.
